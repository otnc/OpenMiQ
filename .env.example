DISCORD_CLIENT_ID=
DISCORD_TOKEN=

# Comma-separated Discord user IDs allowed to run /admin commands.
ADMIN_IDS=

# Locale used when no user/guild/bot default is set. Must match a file in locales/.
DEFAULT_LOCALE=en

# Where per-user/guild/bot settings are stored as JSON. Defaults to ./data.
DATA_DIR=
